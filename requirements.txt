# Note: cloudscraper is included as a vendored dependency in vendor/cloudscraper/
# (pip version is outdated, so we use a local copy)
fastapi>=0.116.1
uvicorn>=0.35.0
pydantic>=2.11.7
beautifulsoup4>=4.13.5
lxml>=6.0.1
requests>=2.32.5
python-multipart>=0.0.20
aiofiles>=24.1.0